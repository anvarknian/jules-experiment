OPENROUTER_API_KEY="YOUR_ACTUAL_OPENROUTER_API_KEY_HERE"
# The DATABASE_URL for the backend service is configured directly in the docker-compose.yml file.
# If you were to run the backend standalone (outside of Docker Compose), you would need to set it, for example:
# DATABASE_URL="postgresql://jules_user:jules_password@localhost:5432/chat_db"
